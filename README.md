# Ashish – Hotel Booking Website

A full-stack hotel booking website built with React (frontend) and Node.js + Express (backend).

### Setup Backend

```bash
cd backend
npm install
npm run dev
```

Server runs at: `http://localhost:5000`

### Setup  Frontend

```bash
cd frontend
npm install
npm start
```

Frontend runs at: `http://localhost:3000`
